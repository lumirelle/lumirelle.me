amends "package://github.com/jdx/hk/releases/download/v1.46.0/hk@1.46.0#/Config.pkl"

import "package://github.com/jdx/hk/releases/download/v1.46.0/hk@1.46.0#/Builtins.pkl"

local linters = new Mapping<String, Step> {
  // Configurations
  ["stylua"] = (Builtins.stylua) {
    glob = List("**/*.lua")
  }
  ["pkl"] = (Builtins.pkl) {
    glob = List("**/*.pkl")
  }
  ["pkl-format"] = (Builtins.pkl_format) {
    glob = List("**/*.pkl")
    shell = null
    fix = "pkl format --write {{ files }}"
  }

  // Source code
  ["oxlint"] = (Builtins.ox_lint) {
    check = "bun run oxlint --deny-warnings {{ files }}"
    fix = "bun run oxlint --deny-warnings --fix {{ files }}"
  }
  ["eslint"] = (Builtins.eslint) {
    batch = false
    glob =
      List(
        // JS & TS
        "*.js",
        "*.mjs",
        "*.cjs",
        "*.jsx",
        "*.ts",
        "*.mts",
        "*.cts",
        "*.tsx",
        // Markdown
        "*.md",
        "*.mdc",
        "*.mdx",
        // JSON
        "*.json",
        "*.jsonc",
        // YAML
        "*.yaml",
        "*.yml",
        // TOML
        "*.toml",
        // Vue
        "*.vue",
      )
    check = "bun run eslint {{ files }}"
    fix = "bun run eslint --fix {{ files }}"
    depends = List("oxlint")
  }
  ["tsc"] = (Builtins.tsc) {
    check = "bun run vue-tsc --build --noEmit"
  }

  // Unused
  ["knip"] = (Builtins.knip) {
    check = "bun run knip-bun --no-progress --directory {{workspace}}"
    // Disable knip auto-fix, because it's dangerous
    fix = null
  }
}

hooks {
  ["pre-commit"] {
    fix = true
    stash = "git"
    steps = linters
  }
  ["fix"] {
    fix = true
    steps = linters
  }
  ["check"] {
    steps = linters
  }
}
